step 1 :       START WITH app.ts
            ==========================
    --> Initialize Express
    --> Enable JSON parsing
    --> Enable CORS
    --> Health check route (/health)
    --> Export app (clean for testing & server start)
Every backend feature (auth, tasks, users, activity logs) hangs on this file. If this is clean, everything else becomes easy mode.

step 2 :       START WITH app.ts
            ==========================